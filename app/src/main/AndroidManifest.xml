<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.baozs.demos.sqlbritedemo2017"> <!-- 覆盖依赖库的sdk设置 -->
    <uses-sdk tools:overrideLibrary="com.baozs.devkit.common" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".V2PostsActivity"
            android:label="@string/title_activity_v2_posts"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".listview.ListViewActivity"></activity>
    </application>

</manifest>